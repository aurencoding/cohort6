// reverse a string
// strings are immutable

// make function
// take in a strong
// convert string to array

// make loop
// make an empty array to hold the new values
// convert that back to a string


function reverseString(input){
    // console.log(input)
    let split = input.split("")
    for ( let i = 0; i < split.length ; i++){
        console.log(i, split[i])
    }
}
console.log(input)